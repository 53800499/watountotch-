/** @format */

/* import { Link } from "react-router-dom";
import { Image } from "../../../../common/components/Image";
import type { HeroSliderInterface } from "../../interfaces/HeroSliderInterface";

export const HeroSlider = ({
  imageSrc,
  country,
  city,
  description,
  buttonText,
  buttonLink
}: HeroSliderInterface) => {
  return (
    <div className="slide">
      <Image
        src={imageSrc || "assets/images/backgrounds/santorini-grecia.png"}
        alt="Santorini, Grecia"
      />
      <div className="slide-info">
        <h2 className="country">{country}</h2>
        <h3 className="city">{city}</h3>
        <p className="text">{description}</p>
        <Link
          to={buttonLink || "javascript:void(0)"}
          className="btn btn-primary">
          {buttonText}
        </Link>
      </div>
    </div>
  );
};
 */